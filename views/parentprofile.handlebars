<head><style>
    label{
        font-weight:bold

    }
    
    </style></head>
<div class="container">
    <div class="row">
        <div class="col-sm-12 col-md-6 col-lg-6">
            <form class="mb-3 disabledinput"
                  id="updateChild"
                  method="post"
                  action="/api/child/{{result.id}}">
                <div class="form-row">
                    <div class="form-group col-md-6">
                        
                            <label for="name-first">First Name</label>
                            <input type="text"
                                   class="form-control disabledinput"
                                   id="child-name-first"
                                   name="name_first"
                                   value="{{result.name_first}}"
                                   disabled />
                        
                    </div>
                    <div class="form-group col-md-6">
                       
                            <label for="name-first">Last Name</label>
                            <input type="text"
                                   class="form-control disabledinput"
                                   id="child-name-last"
                                   name="name_last"
                                   value="{{result.name_last}}"
                                   disabled />
                        
                    </div>
                </div>
                <div class="form-group">
                    <label for="address">Address</label>
                    <input type="text"
                           class="form-control disabledinput"
                           id="address"
                           name="address"
                           value="{{result.address}}"
                           disabled />
                    <label for="city">City</label>
                    <input type="text"
                           class="form-control disabledinput"
                           id="city"
                           name="city"
                           value="{{result.city}}"
                           disabled />
                    <label for="diet">State</label>
                    <input type="text"
                           class="form-control disabledinput"
                           id="state"
                           name="state"
                           value="{{result.state}}"
                           disabled />
                    <label for="diet">Zip</label>
                    <input type="text"
                           class="form-control disabledinput"
                           id="zip"
                           name="zip"
                           value="{{result.zip}}"
                           disabled />
                    <label for="diet">Phone</label>
                    <label for="diet">Phone</label>

                    <input type="text"
                           class="form-control disabledinput"
                           id="phone"
                           name="phone"
                           value="{{result.phone}}"
                           disabled />
                    <label for="diet">email</label>
                    <input type="text"
                           class="form-control disabledinput"
                           id="email"
                           name="email"
                           value="{{result.email}}"
                           disabled />
                </div>
               
            </form>
            <!--<button id="edit" class="btn btn-primary">Edit Child</button>-->
        </div>
        <div class="row">
            <!--<div class="col">
                <form method="post" action="/api/child/del/{{result.id}}">
                <button action="submit" class="btn btn-danger btn-sm" id="deleteChild">Delete Child</button>
                </form>
            </div>-->
        </div>
    </div>
    <a href="/landing">Go to Week View</a>
</div>"
<script type="text/javascript"
        src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>

